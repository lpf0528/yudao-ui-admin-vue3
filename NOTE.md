src\views\zc\salesorder\SalesOrderForm.vue

如果订单没有确认时间（即定时是否确认，不根据状态判断，根据是否存在确认时间判断），则将新增收款、销售单、加工单、水洗码、发货联置灰，并给出提示请先确认订单。


2、
取消这个提示：订单确认后，将不允许编辑，是否继续？

3、
保存后（存在订单号）：客户不允许编辑
确认订单干后（存在确认时间）：
1、运费、优惠金额、不允许编辑
2、不允许添加窗帘、复制、不允许添加结构、不允许添加用料
用料列表禁止编辑。也不允许删除

