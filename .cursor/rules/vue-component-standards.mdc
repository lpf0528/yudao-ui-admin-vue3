---
description: Vue 组件开发规范（script setup、Props、Emits、注释）
globs: **/*.vue
alwaysApply: false
---

# Vue 组件规范

## 强制要求

- **统一使用 `<script setup lang="ts">`**，禁止 Options API，禁止无 TypeScript 的写法
- Props 使用 `defineProps<{}>()` 泛型形式并标注类型
- Emits 使用 `defineEmits<{}>()` 明确声明事件
- 模板中优先使用 UnoCSS 原子类，避免内联 `style`
- 所有跨目录导入使用 `@/` 别名，**禁止相对路径 `../../`**
- 涉及新增、编辑、删除的按钮必须加 `v-hasPermi` 指令

## 文件顶部注释（必须）

```vue
<!--
 用户管理 - 新增/修改弹窗
 功能：提供用户信息的表单录入，支持新增和编辑两种模式
 使用方：views/system/user/index.vue
-->
```

## script setup 分区注释（必须）

```vue
<script setup lang="ts">
// ======================== 导入与声明 ========================
// ======================== Props / Emits ========================
// ======================== 响应式状态 ========================
// ======================== 生命周期 ========================
// ======================== 数据获取 ========================
// ======================== 事件处理 ========================
</script>
```

## 表单弹窗规范

- 弹窗组件通过 `open(type, id?)` 方法被父组件调用，必须 `defineExpose({ open })`
- 成功后通过 `emit('success')` 通知父组件刷新列表
- 编辑模式从接口获取最新数据（不用列表中的数据，避免数据过期）

## 注释禁忌

- ❌ 不写无意义的重复注释，如 `// 获取用户列表` 对应 `getUserList()`
- ❌ 不提交注释掉的死代码
- ✅ 说明"为什么"（业务决策、非直觉性设计），而非"是什么"
